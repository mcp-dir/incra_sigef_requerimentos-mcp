# Ferramentas

INCRA SIGEF: Requerimentos expõe 1 ferramenta (todas somente leitura).

### 1. `incra_sigef_requerimentos_consultar`
**Input**: `cpf` (opcional), `cnpj` (opcional), `codigo_imovel` (opcional), `protocolo` (opcional), `pagina` (opcional), `login_cpf`, `login_senha`, `pkcs12_cert` (opcional), `pkcs12_pass` (opcional)

INCRA SIGEF: Requerimentos, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar INCRA SIGEF: Requerimentos
```
